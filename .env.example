# OpenSky OAuth2 client credentials.
# Create them at opensky-network.org -> Account -> API client.
# Copy this file to .env.local and fill in the values. .env.local is gitignored.
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
